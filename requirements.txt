numpy>=1.23
streamlit>=1.30
pillow
streamlit-image-coordinates
# PyTorch is required for training/playing with .pth checkpoints.
# Install it manually to match your CUDA/CPU setup (recommended).
# pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
